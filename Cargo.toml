[package]
name = "spanishguy"
version = "0.1.0"
edition = "2021"

[dependencies]
regex = "1.10.3"
ureq = { version = "2.10.1", default-features = false }

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
strip = true
